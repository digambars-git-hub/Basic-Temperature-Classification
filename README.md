# Basic-temperature-predictor
It is a basic temperature prediction linear regression model that can take input as temperature or a list of temperatures and gives out the predictions for them either "High" or "Low"
